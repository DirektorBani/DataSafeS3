# ТЗ: DataSafeS3 v1.1.0 — Подготовка к тегу релиза (agent-ready)

**Version:** 1.0  
**Date:** 2026-07-04  
**Status:** Active  
**Роль:** Агент-разработчик  
**EN canonical:** [v1.1.0-release-tag-prep-tz.md](./v1.1.0-release-tag-prep-tz.md)

---

## 1. Кратко

Довести репозиторий до **готовности к `git tag v1.1.0`**. Агент **не пушит** и **не ставит тег** без явного OK пользователя.

**Scope по умолчанию (Option B):** trust-debt C01–C21 **+** HA v2 CE lab (erasure, leader lock, site replication) с disclaimer «lab, не production multi-AZ». Fallback Option A — только trust-debt — см. EN §2.2.

---

## 2. Критерии готовности

| Гейт | Цель |
|------|------|
| `go test ./...` | PASS |
| gofmt / go vet | PASS |
| Playwright 7 specs | PASS |
| Feature-audit | **0 FAIL**, ≥103/105, ≤2 SKIP |
| HA lab (Option B) | `run-all-ha-tests.ps1 -FreshVolumes` exit 0 |
| CHANGELOG | `## [1.1.0] - дата` |
| Версии | compose / Helm / README → v1.1.0 |
| Git | commits готовы; без secrets и `.env.*` |
| Отчёт | `_local/qa/v1.1.0-tag-readiness-*.md` |

---

## 3. Этапы (R0–R7)

| Этап | Содержание |
|------|------------|
| **R0** | Inventory git; scope-freeze doc в `_local/publishing/` |
| **R1** | Feature-audit **0 FAIL**; fix trash restore, tenant RBAC, MinIO gateway; починить `audit.yml` |
| **R2** | Unit, OpenAPI, Playwright, console build |
| **R3** | HA lab (Option B): `run-all-ha-tests.ps1` |
| **R4** | Доки: CHANGELOG, project-status, architecture, RU scaling, Teams/MFA guides, SECURITY |
| **R5** | Bump версий v1.1.0 |
| **R6** | Git hygiene; push summary; **без push** |
| **R7** | Tag readiness report |

Полные AC, команды PowerShell и таблицы файлов — в [EN spec](./v1.1.0-release-tag-prep-tz.md).

---

## 4. Windows / compose

- `DATASAFE_DATA_ROOT=D:/datasafe-data`
- Всегда `-f docker-compose.local-data.yml` для main stack
- Для v1.1 API: `deploy/docker/storage-server-linux` + `docker-compose.local-binary.yml`

---

## 5. Вне scope агента

Push, tag, multi-host HA, field enc v2, mobile, CI ha-smoke (P2).

---

## 6. Перед тегом (maintainer)

- [ ] Readiness report: **YES**
- [ ] Push summary одобрен пользователем
- [ ] `git tag v1.1.0` → `release.yml` → GHCR + SBOM + cosign

---

*Зеркало EN TZ 2026-07-04.*
