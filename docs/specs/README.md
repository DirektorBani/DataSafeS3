English | **[Русский](#русский)**

# Specs index (public repo)

Internal product TZs, QA reports, and strategy drafts live under **`D:\datasafe_tz\`** (not in this git tree).

See `D:\datasafe_tz\README.md` for layout (`specs\`, `qa\`, `analysis\`, `publishing\`, `strategy\`).

Public user/ops documentation stays in `docs/` (getting-started, operations-guide, administrator-guide, user-guide, CHANGELOG).

Architecture decisions that ship with the product: [architecture/adr/](../architecture/adr/).

---

## Русский

Внутренние ТЗ, QA и strategy-черновики — в **`D:\datasafe_tz\`**, не в публичном git.

См. `D:\datasafe_tz\README.md`. Публичная документация остаётся в `docs/`. ADR продукта: [architecture/adr/](../architecture/adr/).
