# Optional overlay when `docker compose build` fails (e.g. stale Windows proxy).
# Usage: scripts\dev-docker-local-binary.cmd
services:
  storage-server:
    volumes:
      - ../docker/storage-server-linux:/storage-server-bin:ro
      - ../docker/docker-entrypoint.sh:/docker-entrypoint.sh:ro
