global:
  scrape_interval: 15s

scrape_configs:
  - job_name: "storage-server"
    static_configs:
      - targets: ["storage-server:9000"]
    # When STORAGE_METRICS_TOKEN is set on storage-server, add:
    # authorization:
    #   type: Bearer
    #   credentials: "<STORAGE_METRICS_TOKEN>"
